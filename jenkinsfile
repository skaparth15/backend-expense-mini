pipeline {
    agent{
        label "agent"
    }

    stages{
        stage("installing dependinces"){
            steps{
                sh """
                  npm install
                  ls -ltr
                """
            }

        }
    }
}